# careOS Patient Web

React + TypeScript + Vite patient frontend.

Run:

```bash
pnpm install
pnpm --filter @careos/patient-web dev
```

Runs on http://localhost:5175 and connects to http://localhost:4003.
