import { apiClient } from './client'; import type { DoctorAvailability,CreateDoctorAvailabilityInput,UpdateDoctorAvailabilityInput } from '../types/models'; export const availabilityApi={list:async(id:string)=>{const{data}=await apiClient.get<{availabilities?:DoctorAvailability[]}>(`/hospital/doctors/${id}/availabilities`);return data.availabilities??[]},create:async(id:string,p:CreateDoctorAvailabilityInput)=>{const{data}=await apiClient.post<{availability?:DoctorAvailability}>(`/hospital/doctors/${id}/availabilities`,p);return data.availability as DoctorAvailability},update:async(id:string,p:UpdateDoctorAvailabilityInput)=>{const{data}=await apiClient.patch<{availability?:DoctorAvailability}>(`/hospital/availabilities/${id}`,p);return data.availability as DoctorAvailability},remove:async(id:string)=>{await apiClient.delete(`/hospital/availabilities/${id}`)}}
