import { useMutation,useQuery,useQueryClient } from '@tanstack/react-query'; import { doctorsApi } from '../api/doctors'; import type { CreateDoctorInput } from '../types/models'; export function useDoctors(){return useQuery({queryKey:['doctors'],queryFn:doctorsApi.list})} export function useCreateDoctor(){const qc=useQueryClient();return useMutation({mutationFn:(p:CreateDoctorInput)=>doctorsApi.create(p),onSuccess:()=>void qc.invalidateQueries({queryKey:['doctors']})})}
