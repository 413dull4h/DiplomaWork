import { useMutation,useQuery,useQueryClient } from '@tanstack/react-query'; import { appointmentsApi } from '../api/appointments'; import type { AppointmentStatus } from '../types/models'; export function useHospitalAppointments(status?:AppointmentStatus|''){return useQuery({queryKey:['appointments',status||'all'],queryFn:()=>appointmentsApi.list(status)})} export function useHospitalAppointment(id?:string){return useQuery({queryKey:['appointment',id],queryFn:()=>appointmentsApi.get(id!),enabled:Boolean(id)})} const inv=(qc:ReturnType<typeof useQueryClient>,id?:string)=>{void qc.invalidateQueries({queryKey:['appointments']}); if(id)void qc.invalidateQueries({queryKey:['appointment',id]})}; export function useConfirmAppointment(){const qc=useQueryClient();return useMutation({mutationFn:appointmentsApi.confirm,onSuccess:(_,id)=>inv(qc,id)})} export function useCancelAppointment(){const qc=useQueryClient();return useMutation({mutationFn:(v:{id:string;cancellationReason?:string})=>appointmentsApi.cancel(v.id,v.cancellationReason),onSuccess:(_,v)=>inv(qc,v.id)})} export function useCompleteAppointment(){const qc=useQueryClient();return useMutation({mutationFn:appointmentsApi.complete,onSuccess:(_,id)=>inv(qc,id)})} export function useNoShowAppointment(){const qc=useQueryClient();return useMutation({mutationFn:appointmentsApi.noShow,onSuccess:(_,id)=>inv(qc,id)})}
