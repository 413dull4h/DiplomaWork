# careOS Hospital Web
Place this folder at `apps/hospital-web`.
Run from monorepo root:
```bash
pnpm install
pnpm --filter @careos/hospital-web dev
```
Open `http://localhost:5174`.
Hospital API must run on `http://localhost:4002`.
Login: `hospital.admin@careos.com` / `Hospital@12345`.
