{
  "name": "careos",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "db:generate": "pnpm --filter @careos/database prisma:generate",
    "db:migrate": "pnpm --filter @careos/database prisma:migrate",
    "db:seed": "pnpm --filter @careos/database prisma:seed",
    "db:studio": "pnpm --filter @careos/database prisma:studio"
  },
  "devDependencies": {
    "@types/node": "latest",
    "tsx": "latest",
    "typescript": "latest"
  }
}